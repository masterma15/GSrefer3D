# 归档说明

- `gaussian_renderer___init___pre_accel_dc_fix.py`：来自仓库 **初始提交**（`f552b52`）中路径  
  `gaussian-splatting/gaussian-splatting/gaussian_renderer/__init__.py` 的副本，对应在 **3DGS 工作区为 accel 光栅补 dc/sh 拆分之前** 的渲染器写法（与 Inria 原版一致的非 `dc` 分支调用方式）。
- 若需恢复该逻辑，可对照此文件与当前 `3DGS/gaussian-splatting/gaussian_renderer/__init__.py` 的差异。
